<%= form_with(model: photography) do |form| %>
  <% if photography.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(photography.errors.count, "error") %> prohibited this photography from being saved:</h2>

      <ul>
        <% photography.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :photo, style: "display: block" %>
    <%= form.text_field :photo %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
